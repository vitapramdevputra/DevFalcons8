//once we define parameterized consturctor.
//default constructor is NOT available.
Cat2 c = new cat2(); //non-parameterized consturctor will work. THIS IS AVAILABLE BY DEFAULT.
c.color = 'purple';
c.name = 'puma';